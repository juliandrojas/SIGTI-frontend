import { useMemo, useState } from "react";
import api from "../../api/axios";
import { employeeAreas } from "../../data/employeeAreas";
import { computerBrands, computerModels, computerSerials } from "../../data/computerCatalog";
import FeedbackModal from "../../components/FeedbackModal";

const normalize = (value = "") => value.normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase().trim().replace(/\s+/g, " ");
const users = Object.keys(employeeAreas).map((name) => name.replace(/\b\w/g, (letter) => letter.toUpperCase()));
const areas = [...new Set(Object.values(employeeAreas))].sort();
const sizes = ["12 pulgadas", "13 pulgadas", "14 pulgadas", "15.6 pulgadas", "17 pulgadas", "19 pulgadas", "21.5 pulgadas", "23.8 pulgadas", "24 pulgadas"];
const storageOptions = ["", "128 GB", "250 GB", "500 GB", "1 TB", "2 TB"];
const operatingSystems = ["WIN 10 Pro", "WIN 10 Single Lenguaje", "WIN 11 Pro", "WIN 7 Pro", "WIN 8.1 Single Lenguaje", "MacOS", "WIN 11 Single Lenguaje", "WIN 11 Pro For Workstations"];
const initialForm = { company: "Petrocasinos", asset_code: "", ip_address: "", area: "", assigned_user: "", brand: "", model: "", serial_number: "", asset_type: "laptop", processor: "", ram: "8 GB", operating_system: "WIN 11 Pro", hdd: "", ssd: "", nvme: false, screen_size: "14 pulgadas", antivirus: "Sophos", notes: "" };
const companyPrefixes = { Petrocasinos: { laptop: "PPO", tower: "PPC", all_in_one: "PPC" }, Cosecharte: { laptop: "CPO", tower: "CPC", all_in_one: "CPC" } };

const Input = ({ name, label, form, setForm, list, required = false, onChange }) => <div className="col-md-6"><label className="form-label" htmlFor={`computer-${name}`}>{label}</label><input id={`computer-${name}`} className="form-control" name={name} value={form[name]} list={list ? `list-${name}` : undefined} required={required} onChange={onChange || ((event) => setForm((current) => ({ ...current, [name]: event.target.value })))} />{list && <datalist id={`list-${name}`}>{list.map((option) => <option key={option} value={option} />)}</datalist>}</div>;

export default function RegisterComponent({ embedded = false, onSaved, existingItems = [] }) {
  const [form, setForm] = useState(initialForm);
  const [message, setMessage] = useState("");
  const [error, setError] = useState("");
  const [submitting, setSubmitting] = useState(false);
  const brands = useMemo(() => [...new Set([...computerBrands, ...existingItems.map((item) => item.brand).filter(Boolean)])].sort(), [existingItems]);
  const models = useMemo(() => [...new Set([...computerModels, ...existingItems.map((item) => item.model).filter(Boolean)])].sort(), [existingItems]);
  const serials = useMemo(() => [...new Set([...computerSerials, ...existingItems.map((item) => item.serial_number).filter(Boolean)])].sort(), [existingItems]);
  const handleUser = (event) => { const assigned_user = event.target.value; setForm((current) => ({ ...current, assigned_user, area: employeeAreas[normalize(assigned_user)] || "" })); };
  const handleSubmit = async (event) => {
    event.preventDefault(); setError(""); setMessage("");
    const requiredLabels = [["company", "empresa"], ["asset_code", "código EF"], ["ip_address", "IP"], ["area", "área"], ["assigned_user", "usuario"], ["brand", "marca"], ["model", "modelo"], ["serial_number", "serial"], ["processor", "procesador"]];
    const missing = requiredLabels.filter(([field]) => !String(form[field] || "").trim()).map(([, label]) => label);
    if (missing.length || (!form.hdd && !form.ssd)) { setError(`Completa los campos obligatorios: ${[...missing, !form.hdd && !form.ssd ? "HDD o SSD" : ""].filter(Boolean).join(", ")}.`); return; }
    const efCode = form.asset_code.trim().toUpperCase();
    if (!/^EF\d{3,4}$/.test(efCode)) { setError("El código EF debe tener el formato EF seguido de 3 o 4 dígitos, por ejemplo EF1234."); return; }
    const assetCode = `${companyPrefixes[form.company]?.[form.asset_type]}${efCode}`;
    if (existingItems.some((item) => String(item.asset_code || "").trim().toLowerCase() === assetCode.toLowerCase())) { setError("El código de equipo ya está registrado. Verifica el código ingresado."); return; }
    setSubmitting(true);
    try { await api.post("/inventory/items", { ...form, asset_code: assetCode, name: `${form.brand.trim()} ${form.model.trim()}`.trim() }); setForm(initialForm); setMessage(`Equipo registrado correctamente con código ${assetCode}.`); onSaved?.(); }
    catch (err) { setError(err?.response?.data?.message || "No se pudo registrar el equipo."); }
    finally { setSubmitting(false); }
  };

  return <div className={embedded ? "" : "container py-4"}>{!embedded && <><p className="text-uppercase text-primary small fw-semibold mb-1">Gestión de activos</p><h1 className="h3 fw-bold mb-4">Registrar equipo</h1></>}<div className={embedded ? "" : "card shadow-sm border-0"}><div className={embedded ? "" : "card-body"}><FeedbackModal message={message} error={error} onClose={() => { setMessage(""); setError(""); }} /><form onSubmit={handleSubmit} className="row g-3"><div className="col-md-6"><label className="form-label" htmlFor="computer-company">Empresa</label><select id="computer-company" className="form-select" value={form.company} onChange={(event) => setForm({ ...form, company: event.target.value })} required><option value="Petrocasinos">Petrocasinos</option><option value="Cosecharte">Cosecharte</option></select></div><Input name="asset_code" label="Código EF (3 o 4 dígitos)" form={form} setForm={setForm} required /><Input name="ip_address" label="IP" form={form} setForm={setForm} required /><div className="col-md-6"><label className="form-label" htmlFor="computer-area">Área</label><select id="computer-area" className="form-select" value={form.area} onChange={(event) => setForm({ ...form, area: event.target.value })} required><option value="">Selecciona un área</option>{areas.map((area) => <option key={area} value={area}>{area}</option>)}</select></div><Input name="assigned_user" label="Usuario" form={form} setForm={setForm} list={users} required onChange={handleUser} /><Input name="brand" label="Marca" form={form} setForm={setForm} list={brands} required /><Input name="model" label="Modelo" form={form} setForm={setForm} list={models} required /><Input name="serial_number" label="Serial" form={form} setForm={setForm} list={serials} required /><div className="col-md-6"><label className="form-label" htmlFor="computer-asset-type">Tipo de activo</label><select id="computer-asset-type" className="form-select" value={form.asset_type} onChange={(event) => setForm({ ...form, asset_type: event.target.value })}><option value="laptop">Portátil</option><option value="tower">Torre</option><option value="all_in_one">Todo en uno</option></select></div><Input name="processor" label="Procesador" form={form} setForm={setForm} required /><div className="col-md-6"><label className="form-label" htmlFor="computer-ram">RAM</label><select id="computer-ram" className="form-select" value={form.ram} onChange={(event) => setForm({ ...form, ram: event.target.value })}>{["4 GB", "8 GB", "12 GB", "16 GB", "32 GB", "64 GB"].map((value) => <option key={value}>{value}</option>)}</select></div><div className="col-md-6"><label className="form-label" htmlFor="computer-os">Sistema operativo</label><select id="computer-os" className="form-select" value={form.operating_system} onChange={(event) => setForm({ ...form, operating_system: event.target.value })}>{operatingSystems.map((value) => <option key={value}>{value}</option>)}</select></div><div className="col-md-6"><label className="form-label" htmlFor="computer-hdd">HDD</label><select id="computer-hdd" className="form-select" value={form.hdd} onChange={(event) => setForm({ ...form, hdd: event.target.value })}>{storageOptions.map((value) => <option key={value} value={value}>{value || "No aplica"}</option>)}</select></div><div className="col-md-6"><label className="form-label" htmlFor="computer-ssd">SSD</label><select id="computer-ssd" className="form-select" value={form.ssd} onChange={(event) => setForm({ ...form, ssd: event.target.value, nvme: event.target.value ? form.nvme : false })}>{storageOptions.map((value) => <option key={value} value={value}>{value || "No aplica"}</option>)}</select></div><div className="col-md-6 d-flex align-items-end"><div className="form-check mb-2"><input id="computer-nvme" className="form-check-input" type="checkbox" checked={form.nvme} disabled={!form.ssd} onChange={(event) => setForm({ ...form, nvme: event.target.checked })} /><label className="form-check-label" htmlFor="computer-nvme">El SSD es NVMe</label></div></div><div className="col-md-6"><label className="form-label" htmlFor="computer-screen">Tamaño de pantalla</label><select id="computer-screen" className="form-select" value={form.screen_size} onChange={(event) => setForm({ ...form, screen_size: event.target.value })}>{sizes.map((value) => <option key={value}>{value}</option>)}</select></div><div className="col-md-6"><label className="form-label" htmlFor="computer-antivirus">Antivirus</label><select id="computer-antivirus" className="form-select" value={form.antivirus} onChange={(event) => setForm({ ...form, antivirus: event.target.value })}><option>Sophos</option><option>Defender</option></select></div><div className="col-12"><label className="form-label" htmlFor="computer-notes">Observaciones</label><textarea id="computer-notes" className="form-control" rows="3" value={form.notes} onChange={(event) => setForm({ ...form, notes: event.target.value })} /></div><div className="col-12"><button className="btn btn-primary" type="submit" disabled={submitting}>{submitting ? "Guardando..." : "Guardar equipo"}</button></div></form></div></div></div>;
}
